{{#if resume.awards.length}}
<div class="container awards-container">
    {{> title value="Premios"}}

    {{#each resume.awards}}
        <section class="item">
            {{> section-header name=this.title showWebsite=true }}

            {{#if awarder}}
            <h5 class="awarder">{{awarder}}</h5>
            {{/if}}

            {{#if summary}}
            <p class="Resumen">{{summary}}</p>
            {{/if}}
        </section>
    {{/each}}
</div>
{{/if}}